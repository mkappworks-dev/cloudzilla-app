package components

// For inline groups where a <fieldset> + <legend> would feel heavy; prefer the fieldset for form sections.
templ RadioGroup(ariaLabel string, attrs templ.Attributes) {
	<div role="radiogroup" aria-label={ ariaLabel } { withClass(attrs, "grid gap-2")... }>
		{ children... }
	</div>
}
