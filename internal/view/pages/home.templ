package pages

import (
	"strconv"

	"github.com/mkappworks-dev/cloudzilla-app/internal/view"
	"github.com/mkappworks-dev/cloudzilla-app/internal/view/components"
	"github.com/mkappworks-dev/cloudzilla-app/internal/view/layout"
)

templ Home(data view.HomeData) {
	@layout.Base(data.BasePage, "Explore Repositories") {
		<div class="space-y-6">
			<div class="flex justify-between items-center">
				<h1 class="text-3xl font-bold">Repositories</h1>
			</div>
			<nav aria-label="Quick links" class="flex flex-wrap gap-2 text-sm">
				<a href="/explore" class="px-3 py-1.5 bg-white border border-gray-200 rounded-md hover:border-blue-300 hover:text-blue-600 transition-colors">Explore</a>
				<a href="/gists" class="px-3 py-1.5 bg-white border border-gray-200 rounded-md hover:border-blue-300 hover:text-blue-600 transition-colors">Gists</a>
				if data.BasePage.CurrentUser != nil {
					<a href="/feed" class="px-3 py-1.5 bg-white border border-gray-200 rounded-md hover:border-blue-300 hover:text-blue-600 transition-colors">Activity Feed</a>
					<a href="/notifications" class="px-3 py-1.5 bg-white border border-gray-200 rounded-md hover:border-blue-300 hover:text-blue-600 transition-colors">Notifications</a>
					<a href={ templ.SafeURL("/" + data.BasePage.CurrentUser.Username) } class="px-3 py-1.5 bg-white border border-gray-200 rounded-md hover:border-blue-300 hover:text-blue-600 transition-colors">Your profile</a>
					<a href="/settings" class="px-3 py-1.5 bg-white border border-gray-200 rounded-md hover:border-blue-300 hover:text-blue-600 transition-colors">Settings</a>
				}
			</nav>
			if len(data.Repos) > 0 {
				<div class="grid gap-4">
					for _, repo := range data.Repos {
						<a href={ templ.SafeURL("/" + repo.OwnerName + "/" + repo.Name) } class="block bg-white border border-gray-200 rounded-lg p-4 hover:border-blue-300 hover:shadow-md transition-all">
							<h2 class="text-xl font-semibold text-blue-600">{ repo.OwnerName }/{ repo.Name }</h2>
							if repo.Description != "" {
								<p class="text-gray-600 mt-1">{ repo.Description }</p>
							}
							<div class="mt-2 text-xs text-gray-500 flex gap-3">
								<span>
									if repo.Private {
										Private
									} else {
										Public
									}
								</span>
								<span>Updated { repo.UpdatedAt.Format("Jan 2, 2006") }</span>
							</div>
						</a>
					}
				</div>
			} else {
				@components.EmptyState("No repositories yet.")
			}
			if len(data.Templates) > 0 {
				<section class="mt-8">
					<h2 class="text-sm font-semibold text-gray-700 mb-3">Start from a template</h2>
					<div class="grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-3">
						for _, tmpl := range data.Templates {
							<div class="rounded-md border border-gray-200 bg-white p-4">
								<p class="text-sm font-medium text-gray-900">{ tmpl.OwnerName }/{ tmpl.Name }</p>
								if tmpl.Description != "" {
									<p class="text-xs text-gray-500 mt-1">{ tmpl.Description }</p>
								}
								if data.BasePage.CurrentUser != nil {
									<form method="POST" action="/api/repos/from-template" class="mt-3">
										<input type="hidden" name="template_repo_id" value={ strconv.FormatInt(tmpl.ID, 10) }/>
										<input type="text" name="name" placeholder="New repo name" required class="w-full rounded border border-gray-300 px-2 py-1 text-xs focus:outline-none focus:ring-1 focus:ring-indigo-500 mb-2"/>
										<button type="submit" class="w-full rounded bg-indigo-600 px-2 py-1 text-xs font-semibold text-white hover:bg-indigo-700">Use this template</button>
									</form>
								}
							</div>
						}
					</div>
				</section>
			}
		</div>
	}
}
