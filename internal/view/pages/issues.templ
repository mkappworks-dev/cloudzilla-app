package pages

import (
	"strconv"

	"github.com/mkappworks-dev/cloudzilla-app/internal/view"
	"github.com/mkappworks-dev/cloudzilla-app/internal/view/components"
	"github.com/mkappworks-dev/cloudzilla-app/internal/view/layout"
)

templ Issues(data view.IssuesData) {
	@layout.Base(data.BasePage, "Issues · "+data.Owner+"/"+data.RepoName) {
		<div class="space-y-6">

			<section class="flex flex-wrap items-start justify-between gap-4" aria-labelledby="issues-title">
				<div class="min-w-0">
					<p class="font-mono text-[11px] text-muted-foreground uppercase tracking-wider mb-2">
						<a href={ templ.SafeURL("/" + data.Owner + "/" + data.RepoName) } class="hover:underline">{ data.RepoName }</a>
						<span aria-hidden="true">·</span>
						Issues
					</p>
					<h1 id="issues-title" class="text-2xl font-semibold tracking-tight">Issues</h1>
				</div>
				@components.LinkButton("/"+data.Owner+"/"+data.RepoName+"/issues/new", components.ButtonSuccess, components.ButtonSizeDefault, nil) {
					<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true" focusable="false">
						<path d="M12 5v14M5 12h14"></path>
					</svg>
					New issue
				}
			</section>

			if len(data.PinnedIssues) > 0 {
				<section aria-labelledby="pinned-heading">
					<h2 id="pinned-heading" class="font-mono text-[11px] text-muted-foreground uppercase tracking-wider mb-2">
						Pinned
						<span class="ml-1">{ strconv.Itoa(len(data.PinnedIssues)) }</span>
					</h2>
					<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3">
						for _, pinned := range data.PinnedIssues {
							<a href={ templ.SafeURL("/" + data.Owner + "/" + data.RepoName + "/issues/" + strconv.Itoa(pinned.Number)) } class="block rounded-md border border-warning/40 bg-warning/10 p-4 hover:border-warning/60 transition">
								<div class="flex items-start gap-2">
									<svg class="w-4 h-4 text-warning shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true" focusable="false">
										<path d="M10 1l2.39 4.84 5.34.78-3.87 3.77.91 5.32L10 13.27l-4.77 2.51.91-5.32L2.27 6.62l5.34-.78L10 1z"></path>
									</svg>
									<div class="min-w-0">
										<p class="text-[13px] font-semibold truncate">{ pinned.Title }</p>
										<p class="mt-1 font-mono text-[11px] text-muted-foreground">#{ strconv.Itoa(pinned.Number) } · { string(pinned.State) }</p>
									</div>
								</div>
							</a>
						}
					</div>
				</section>
			}

			if len(data.Issues) > 0 {
				<section class="rounded-md border border-border bg-card overflow-hidden" aria-labelledby="issue-list">
					<h2 id="issue-list" class="sr-only">Issues</h2>
					<ol class="divide-y divide-border">
						for _, issue := range data.Issues {
							<li>
								<a href={ templ.SafeURL("/" + data.Owner + "/" + data.RepoName + "/issues/" + strconv.Itoa(issue.Number)) } class="flex items-start gap-3 px-4 py-3 hover:bg-muted/50">
									if string(issue.State) == "open" {
										<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" class="shrink-0 mt-1 text-success" aria-hidden="true" focusable="false">
											<circle cx="12" cy="12" r="9"></circle>
											<circle cx="12" cy="12" r="3"></circle>
										</svg>
									} else {
										<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" class="shrink-0 mt-1 text-destructive" aria-hidden="true" focusable="false">
											<circle cx="12" cy="12" r="9"></circle>
											<path d="M9 9l6 6M15 9l-6 6"></path>
										</svg>
									}
									<div class="min-w-0 flex-1">
										<div class="text-[13px] font-medium">
											if issue.Visibility == "private" {
												<span class="inline-flex items-center mr-1 text-warning" title="Private" aria-label="Private">🔒</span>
											}
											<span class="hover:underline">{ issue.Title }</span>
										</div>
										<div class="mt-1 flex flex-wrap items-center gap-1.5 text-[12px] text-muted-foreground">
											<span class="font-mono">#{ strconv.Itoa(issue.Number) }</span>
											<span aria-hidden="true">·</span>
											<span>opened by { issue.AuthorName }</span>
											if labels, ok := data.IssueLabels[issue.ID]; ok && len(labels) > 0 {
												<span class="ml-2 inline-flex flex-wrap gap-1" aria-label="Labels">
													for _, l := range labels {
														<span class="px-2 py-0.5 rounded-full text-[11px] border" style={ "background-color: " + l.Color + "33; color: " + l.Color + "; border-color: " + l.Color + "55" }>{ l.Name }</span>
													}
												</span>
											}
										</div>
									</div>
									<div class="shrink-0">
										@components.IssueStateBadge(string(issue.State))
									</div>
								</a>
							</li>
						}
					</ol>
				</section>
			} else {
				@components.EmptyState("No issues yet.")
			}
		</div>
	}
}
