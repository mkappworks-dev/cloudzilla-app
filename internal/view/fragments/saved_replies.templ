package fragments

import (
	"strconv"
	"github.com/mkappworks/cloudzilla/internal/view"
)

templ SavedRepliesList(data view.SavedRepliesFragData) {
	<div id="saved-replies-list">
		if len(data.Replies) > 0 {
			<div class="space-y-3">
				for _, reply := range data.Replies {
					<div class="bg-white border border-gray-200 rounded-lg p-4" id={ "reply-" + strconv.FormatInt(reply.ID, 10) }>
						<div class="flex justify-between items-start gap-4">
							<div class="min-w-0 flex-1">
								<p class="font-semibold text-sm text-gray-900">{ reply.Title }</p>
								<p class="text-sm text-gray-600 mt-1 whitespace-pre-wrap">{ reply.Body }</p>
							</div>
							<div class="flex gap-2 shrink-0">
								<button
									x-data
									x-on:click={ "$el.closest('[id^=reply-]').querySelector('[id^=edit-form-]').classList.toggle('hidden')" }
									class="text-xs text-blue-600 hover:underline">
									Edit
								</button>
								<button
									hx-delete={ "/api/user/replies/" + strconv.FormatInt(reply.ID, 10) }
									hx-target="#saved-replies-list"
									hx-swap="outerHTML"
									hx-confirm="Delete this saved reply?"
									class="text-xs text-red-600 hover:underline">
									Delete
								</button>
							</div>
						</div>
						<div id={ "edit-form-" + strconv.FormatInt(reply.ID, 10) } class="hidden mt-4 space-y-3 border-t border-gray-100 pt-4">
							<form
								hx-patch={ "/api/user/replies/" + strconv.FormatInt(reply.ID, 10) }
								hx-target="#saved-replies-list"
								hx-swap="outerHTML"
								class="space-y-3">
								<input type="text" name="title" value={ reply.Title }
									class="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
									required/>
								<textarea name="body" rows="3"
									class="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
									required>{ reply.Body }</textarea>
								<div class="flex gap-2">
									<button type="submit" class="px-3 py-1.5 bg-blue-600 text-white rounded text-sm hover:bg-blue-700">Save</button>
									<button type="button"
										x-data
										x-on:click={ "$el.closest('[id^=reply-]').querySelector('[id^=edit-form-]').classList.add('hidden')" }
										class="px-3 py-1.5 bg-gray-100 text-gray-700 rounded text-sm hover:bg-gray-200">Cancel</button>
								</div>
							</form>
						</div>
					</div>
				}
			</div>
		} else {
			<div class="text-center py-8 border border-gray-200 border-dashed rounded-lg bg-gray-50">
				<p class="text-gray-500">No saved replies yet. Create one above.</p>
			</div>
		}
	</div>
}

templ SavedRepliesPicker(data view.SavedRepliesPickerFragData) {
	<div id="saved-replies-picker-container" class="relative">
		<div id="saved-replies-picker" class="absolute z-50 right-0 mt-1 w-72 bg-white border border-gray-200 rounded-lg shadow-lg max-h-64 overflow-y-auto">
			if len(data.Replies) > 0 {
				<ul class="py-1">
					for _, reply := range data.Replies {
						<li>
							<button
								type="button"
								class="w-full text-left px-4 py-2 hover:bg-gray-50 text-sm"
								data-saved-reply-body={ reply.Body }>
								<span class="font-medium text-gray-900">{ reply.Title }</span>
								<span class="block text-xs text-gray-500 truncate mt-0.5">{ reply.Body }</span>
							</button>
						</li>
					}
				</ul>
			} else {
				<p class="px-4 py-3 text-sm text-gray-500">No saved replies. <a href="/settings/replies" class="text-blue-600 hover:underline">Create one</a>.</p>
			}
		</div>
	</div>
}
