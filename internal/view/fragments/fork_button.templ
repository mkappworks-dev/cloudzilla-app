package fragments

import (
	"strconv"

	"github.com/mkappworks-dev/cloudzilla-app/internal/view"
)

templ ForkButton(data view.ForkButtonData) {
	<div id="fork-button" class="flex items-center gap-1">
		if data.BasePage.CurrentUser != nil {
			<button
				hx-post={ "/api/repos/" + data.Owner + "/" + data.RepoName + "/fork" }
				hx-target="#fork-button"
				hx-swap="outerHTML"
				class="inline-flex items-center gap-1 px-3 py-1.5 border border-border bg-background text-foreground rounded text-sm font-medium hover:bg-muted/40 transition-colors"
			>
				&#x2442; Fork
			</button>
		} else {
			<a href="/login" class="inline-flex items-center gap-1 px-3 py-1.5 border border-border bg-background text-foreground rounded text-sm font-medium hover:bg-muted/40 transition-colors">
				&#x2442; Fork
			</a>
		}
		<span class="px-2 py-1.5 border border-border bg-background text-foreground rounded text-sm">
			{ strconv.Itoa(data.ForkCount) }
		</span>
	</div>
}
