package fragments

import "github.com/mkappworks-dev/cloudzilla-app/internal/view"

templ BranchesList(data view.BranchesFragData) {
	<div id="branches-list">
		if len(data.Branches) > 0 {
			for _, branch := range data.Branches {
				<div class="flex items-center justify-between px-4 py-3 hover:bg-gray-50 border-b border-gray-100 last:border-b-0">
					<div class="flex items-center gap-2 min-w-0">
						<a href={ templ.SafeURL("/" + data.Owner + "/" + data.RepoName + "/tree/" + branch.Name) } class="font-mono text-sm text-blue-600 hover:underline truncate">{ branch.Name }</a>
						if branch.IsDefault {
							<span class="text-xs bg-green-100 text-green-700 px-2 py-0.5 rounded shrink-0">default</span>
						}
						<span class="text-xs text-gray-400 font-mono shrink-0">{ branch.Hash }</span>
					</div>
					<div class="flex items-center gap-3 ml-4 shrink-0">
						<a href={ templ.SafeURL("/" + data.Owner + "/" + data.RepoName + "/commits/" + branch.Name) } class="text-xs text-gray-500 hover:text-blue-600">commits</a>
						if data.CanWrite && !branch.IsDefault {
							<button
								hx-delete={ "/api/repos/" + data.Owner + "/" + data.RepoName + "/branches?name=" + branch.Name }
								hx-target="#branches-list"
								hx-swap="outerHTML"
								hx-confirm={ "Delete branch '" + branch.Name + "'?" }
								class="text-xs text-red-600 hover:text-red-800 border border-red-200 px-2 py-1 rounded hover:bg-red-50"
							>Delete</button>
						}
					</div>
				</div>
			}
		} else {
			<div class="px-4 py-8 text-center text-gray-500 text-sm">No branches yet.</div>
		}
	</div>
}

templ TagsList(data view.TagsFragData) {
	<div id="tags-list">
		if len(data.Tags) > 0 {
			for _, tag := range data.Tags {
				<div class="flex items-center justify-between px-4 py-3 hover:bg-gray-50 border-b border-gray-100 last:border-b-0">
					<div class="flex items-center gap-2 min-w-0">
						<a href={ templ.SafeURL("/" + data.Owner + "/" + data.RepoName + "/tree/" + tag.Name) } class="font-mono text-sm text-blue-600 hover:underline truncate">{ tag.Name }</a>
						<span class="text-xs text-gray-400 font-mono shrink-0">{ tag.Hash }</span>
					</div>
					<div class="flex items-center gap-3 ml-4 shrink-0">
						<a href={ templ.SafeURL("/" + data.Owner + "/" + data.RepoName + "/commits/" + tag.Name) } class="text-xs text-gray-500 hover:text-blue-600">commits</a>
						if data.CanWrite {
							<button
								hx-delete={ "/api/repos/" + data.Owner + "/" + data.RepoName + "/tags?name=" + tag.Name }
								hx-target="#tags-list"
								hx-swap="outerHTML"
								hx-confirm={ "Delete tag '" + tag.Name + "'?" }
								class="text-xs text-red-600 hover:text-red-800 border border-red-200 px-2 py-1 rounded hover:bg-red-50"
							>Delete</button>
						}
					</div>
				</div>
			}
		} else {
			<div class="px-4 py-8 text-center text-gray-500 text-sm">No tags yet.</div>
		}
	</div>
}
