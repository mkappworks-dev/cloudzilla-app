version: "2"
sql:
  - engine: "sqlite"
    queries: "internal/store/query/"
    schema: "migrations/"
    gen:
      go:
        package: "db"
        out: "internal/store/db"
        emit_json_tags: true
        emit_empty_slices: true
        emit_exact_table_names: false
